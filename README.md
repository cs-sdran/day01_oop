第一天，数组，方法，面向对象
数组类型[] 数组名=new 数组类型[];
栈区 堆区 方法区
alt insert快捷使用构造器，set get 方法
this作用，区分类内成员变量和方法中的局部变量
就近原则
this的本质，表示方法调用者的内存地址
静态变量：被当前类所有对象共享，只要赋值一次，类名.静态变量赋值，静态变量随着类的产生而产生，JDK8后处于堆内存中，静态变量先于对象出现
static修饰的方法叫静态方法，通常不用在javabean类(描述一类事物)中，用在测试类和工具类(帮我们做一些事情的类，私有化构造方法是为了不让外界创建对象，只能使用类中的方法)中
静态方法可以不创建对象即可在另一个类里使用，实例方法可以调用静态方法，静态方法也可调用静态方法，静态方法不能调用实例方法
ctrl+alt+v 自动生成左边接受方法
常量由final修饰，只能赋值一次，不可修改
基本数据类型变量记录的是真实的数据，引用数据类型变量记录的是对象的内存地址
枚举类的对象是有限个的，每个枚举项默认使用public static final修饰，可以使用类名.调用，枚举项以逗号隔开，分号结尾，枚举类的构造方法必须是private，不让外界创建对象
枚举类的每个对象都是通过构造方法构造出来的
